import Button from './button'
import Input from './input'

const SearchForm = props => {
    return (
        <form>
            <Button>
                Search
            </Button>
        </form>
    )
}

export default SearchForm